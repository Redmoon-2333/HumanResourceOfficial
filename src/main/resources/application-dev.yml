spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrofficial?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

  mail:
    host: smtp.163.com
    port: 465
    username: 13136397281@163.com
    password: RMnuLHrnM9KXQvKt
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: "true"
        transport:
          protocol: smtps

  data:
    redis:
      host: localhost
      port: 6379
      password: RedMoon

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.redmoon2333: DEBUG
    org.springframework.ai: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

aliyun:
  oss:
    endpoint: oss-cn-beijing.aliyuncs.com
    accessKeyId: ${ALIYUN_OSS_ACCESS_KEY_ID:}
    accessKeySecret: ${ALIYUN_OSS_ACCESS_KEY_SECRET:}
    bucketName: ${ALIYUN_OSS_BUCKET_NAME:}

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always
